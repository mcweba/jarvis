Strategies {
        default = everyUpdate
}

Items {
    // persist all items once a day and on every change and restore them from the db at startups
    gMapDB* : strategy = everyChange, restoreOnStartup
}