Strategies {
    every5Minutes    : "0 */5 * * * ?"
    everyHour        : "0 0 * * * ?"
    everyDay         : "0 0 0 * * ?"

    default          = everyChange
}

Items {
    gLights*                      : strategy = everyChange
}
